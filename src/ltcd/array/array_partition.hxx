#ifndef ARRAY_PARTITION_HXX
#define ARRAY_PARTITION_HXX

class ArrayPartition
{
public:
    ArrayPartition() = default;
    ~ArrayPartition() = default;
    void solution(const int & target, int * array, const int & length);
};

#endif
